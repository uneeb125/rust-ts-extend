The compartments on the left-hand side and right-hand side of an expression
do not match.
For example:
# Example 1
#[compartments(a)]
fn foo(x: i32) -> i32 {
    x + 1
}
#[compartments(b)]fn bar(x: i32) -> i32 {
    x + 1
}
fn main() {
    foo(42) + bar(42)
}
