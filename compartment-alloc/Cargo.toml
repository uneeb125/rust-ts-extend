[workspace]

[package]
name = "compartment-alloc"
version = "0.1.0"
edition = "2021"
description = "Compartment-tagged global allocator for Rust compartment system"
license = "MIT OR Apache-2.0"

[dependencies]
